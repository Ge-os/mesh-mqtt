# =============================================================================
# Meshtastic MQTT Time Service – Environment Variables
# Copy this file to .env and fill in your values.
# =============================================================================

# -----------------------------------------------------------------------------
# Server / Public IP
# -----------------------------------------------------------------------------
# The public IP address (or domain name) of this server.
# Meshtastic devices will connect to this address.
SERVER_PUBLIC_IP=203.0.113.10

# -----------------------------------------------------------------------------
# Mosquitto Broker
# -----------------------------------------------------------------------------
# Host and port that the Python service uses to reach the local broker.
# Inside Docker Compose this is the service name "mosquitto".
MQTT_HOST=mosquitto
MQTT_PORT=1883

# Credentials for the MQTT broker.
# These must match what you set with mosquitto_passwd (see setup guide).
MQTT_USERNAME=meshuser
MQTT_PASSWORD=changeme_strong_password

# -----------------------------------------------------------------------------
# Time Service
# -----------------------------------------------------------------------------
# Timezone for the time reply (IANA tz database name).
# Examples: UTC  Europe/Berlin  America/New_York  Asia/Tokyo
TIMEZONE=UTC

# The trigger word/phrase (case-insensitive) that activates the time reply.
# A mesh node must send a text message containing this exact string.
TIME_TRIGGER=!time
